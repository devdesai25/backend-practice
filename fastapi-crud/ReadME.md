# FastAPI CRUD App

Basic CRUD API built with FastAPI, SQLAlchemy, and PostgreSQL (Supabase).

## Features
- Create, Read, Update, Delete items
- PostgreSQL database
- SQLAlchemy ORM
- Swagger UI

## Run locally
1. Create virtual environment
2. Add `.env` with DATABASE_URL
3. Run `uvicorn main:app --reload`
